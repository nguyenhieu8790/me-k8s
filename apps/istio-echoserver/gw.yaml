apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: echo-server
spec:
  selector:
    gw-type: istio-ingress
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - echo.abc.com